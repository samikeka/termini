# Screenshots

PNG previews for the root README:

| File | Screen |
|------|--------|
| `home.png` | Home — public matches & field list |
| `booking.png` | Booking flow — time slots & confirm |
| `owner-dashboard.png` | Owner panel — revenue & calendar |

## Source

These were generated from the static UI prototype at `termini.html` (no backend/frontend server required).

## Refresh from the live app

1. Start the app (see root `README.md`).
2. Capture pages at `http://localhost:5173`.
3. Overwrite the PNGs in this folder.
4. Commit the updated images.
